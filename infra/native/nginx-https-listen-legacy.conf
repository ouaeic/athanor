# HTTPS listeners for nginx older than 1.25.1, where the http2 directive does not
# exist yet and HTTP/2 is enabled as a listen parameter.
listen 443 ssl http2 default_server;
listen [::]:443 ssl http2 default_server;
